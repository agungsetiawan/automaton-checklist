<h2>Edit Task</h2>

<%= form_for([@project,@project.tasks.find(params[:id])]) do |f| %>
  <div>
    <%= f.label :task %> </br>
    <%= f.text_area :body %>
  </div>

  <div>
    <%= f.submit %>
  </div>
<% end %>

<%= link_to "Back",@project %>